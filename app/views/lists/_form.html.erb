<%= form_for @list, remote: true do |f| %>
  <div class="field">	
		<%= f.label :name %><br />
		<%= f.text_field :name %>
		<%= f.hidden_field :user_id, value: current_user.id  %>
		<%= f.submit %>
<% end %>